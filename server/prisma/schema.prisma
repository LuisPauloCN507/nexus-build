generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Game {
  id         String      @id @default(uuid())
  name       String
  slug       String      @unique
  logoUrl    String
  characters Character[]
}

model Character {
  id                String   @id @default(uuid())
  name              String
  tier              String
  imageUrl          String
  buildDetails      String
  strategy          String?
  
  // Status
  speed             String?
  stamina           String?
  power             String?
  guts              String?
  wisdom            String?
  
  // Guia com Imagens (Strings separadas por vírgula para os nomes e para os ícones)
  recommendedSkills String?
  skillIcons        String? // Links das imagens das skills
  supportCards      String?
  cardIcons         String? // Links das imagens dos cards
  
  sourceName        String
  sourceUrl         String
  game              Game     @relation(fields: [gameId], references: [id])
  gameId            String
}